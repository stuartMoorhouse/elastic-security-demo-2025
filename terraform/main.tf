# AWS VM resources disabled for Detection as Code demo
# Uncomment these resources when ready to add purple team exercise VMs

# All AWS infrastructure (VPC, subnets, security groups, EC2 instances) commented out
# For Detection as Code demo, only Elastic Cloud and GitHub resources are active

# To re-enable AWS VMs for purple team exercise:
# 1. Uncomment all resources below
# 2. Run: terraform init && terraform apply
